{
    "version": 1,
    "builds": [{ "src": "chatbot.py", "use": "@vercel/python" }],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ],
    "env": {
      "FLASK_ENV": "production"
    }
  }